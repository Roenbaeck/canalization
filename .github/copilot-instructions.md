# Copilot Instructions

## LaTeX Environment

There is no local LaTeX compiler (pdflatex, latexmk, etc.) installed on this machine. The author uses **OpenAI Prism** (online) to render `.tex` files to PDF. Do not attempt to compile LaTeX locally.
